# MVP技術スタック候補と採用案（自己啓発・習慣トラッカーWebアプリ）

## 候補1: Python + Flask + SQLite + プレーンHTML/CSS/JS
- **言語・フレームワーク**: Python / Flask
- **データ保存**: SQLite（`sqlite3`）
- **メリット**:
  - 学習コストが低く、最速でMVPを動かしやすい
  - 標準ライブラリでSQLiteを扱えるため依存が少ない
  - テンプレート＋軽量JSで要件に十分対応可能
- **デメリット**:
  - 大規模化時は構成を整理し直す必要がある

## 候補2: Node.js + Express + SQLite + プレーンHTML/CSS/JS
- **言語・フレームワーク**: JavaScript / Express
- **データ保存**: SQLite（`better-sqlite3` など）
- **メリット**:
  - フロントとバックで言語統一しやすい
  - API拡張・将来のSPA化に繋げやすい
- **デメリット**:
  - パッケージ選定が多く、初期判断が増える

## 候補3: Go + net/http + SQLite + サーバーサイドテンプレート
- **言語・フレームワーク**: Go / net/http（またはGin）
- **データ保存**: SQLite
- **メリット**:
  - 単一バイナリ配布しやすく、実行が安定
  - 型安全で保守性が高い
- **デメリット**:
  - UIの素早い試作ではPython/Nodeより記述量が増えやすい

## 今回の採用スタック（結論）
**Python + Flask + SQLite + プレーンHTML/CSS/JS** を採用します。

### 採用理由
1. 「まずローカルで動くMVP」を最短で実現しやすい。
2. 習慣CRUD・日次チェック・週次一覧はFlask + SQLiteで過不足なく実装できる。
3. 将来的に認証・マルチユーザー対応へ段階的に拡張しやすい。

## 次ステップ（実装開始時）
次はこの採用スタック前提で、ディレクトリ構成と主要ファイルを「コピペで動く」形で提示し、ローカル起動まで進めます。
